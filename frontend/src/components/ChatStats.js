// themes, common words, common phrases, uncommon

let ChatStats = props => {

    return(
        <div className="panelCard"> 
            I am Chat stats
        </div>
    );
}

export default ChatStats;