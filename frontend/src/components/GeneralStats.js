// word count, msg count

let GeneralStats = props => {

    let wordCount;
    let msgCount;

    return(
        <div className="panelCard">
            I am general stats
        </div>
    );
}

export default GeneralStats;
